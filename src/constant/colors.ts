const commonTheme = {
    mainColor: '',
};

const darkTheme = {
    ...commonTheme,
    text: '#fff',
    background: '#000',
};

const lightTheme = {
    ...commonTheme,
    text: '#000',
    background: '#fff',
};

export { darkTheme, lightTheme };
