import React from 'react';
import CustomText from '../common/CustomText';
import { ModalType } from '../../@types/models/modal';

type Props = {};

const Dialog = (props: ModalType) => {
    const {} = props;

    return <CustomText>dialog</CustomText>;
};

export default Dialog;
